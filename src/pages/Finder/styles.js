import styled from 'styled-components'

export const Footer = styled.div`
  position: absolute;
  bottom: var(--small);
  right: 0;
  left: 0;
  display: flex;
  justify-content: center;
  width: 100%
`
