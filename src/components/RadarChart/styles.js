import styled from 'styled-components'

export const Container = styled.div`
  width: 100%;
  height: 180px;
  text-shadow: .5px .5px 1px var(--grayscale-3);
  filter: drop-shadow(-10px 10px 4px var(--blue));
`